# Unpredictable Weather, Conditionals, Ruby Basics, Exercises

sun = ['visible', 'hidden'].sample

# Pt. 1
#puts 'The sun is so bright' if sun == 'visible'

# Pt. 2
#puts 'The clouds are blocking the sun!' unless sun == 'visible'

# Pt. 3
puts 'The sun is so bright!' if sun == 'visible'
puts 'The clouds are blocking the sun!' unless sun == 'visible'
